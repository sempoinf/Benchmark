#pragma once

#include <math.h>
#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int* arr_gen(int);
void merge_sort(int*, int, int);
void selection_sort(int*, int);
void single_array(void);
void benchmark(void);
